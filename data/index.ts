import { landingPageData } from './landing-page-text'
import { weeklyScheduleData } from './weekly-schedule-mock'
import { mobileScheduleData, desktopScheduleData } from './schedule-text'

// Mock data

const data = {
  landingPageData,
  mobileScheduleData,
  desktopScheduleData,
  weeklyScheduleData,
}

export default data
